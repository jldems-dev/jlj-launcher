// Renderer code is split across src/renderer/core, src/renderer/features,
// and src/renderer/shared. index.html loads those classic scripts in order
// so existing inline handlers and frontend behavior keep their global API.
